# Docker Environment Configuration Template for AI-Playground Frontend
# Copy this file to .env and update values as needed

# =============================================================================
# API CONFIGURATION
# =============================================================================
# For Docker Compose (backend service accessible from host)
VITE_API_URL=http://localhost:8000

# For production (use your actual domain)
# VITE_API_URL=https://api.yourdomain.com

# =============================================================================
# WEBSOCKET CONFIGURATION
# =============================================================================
# For Docker Compose (backend service accessible from host)
VITE_WS_URL=ws://localhost:8000/ws

# For production (use your actual domain)
# VITE_WS_URL=wss://api.yourdomain.com/ws

# =============================================================================
# ENVIRONMENT
# =============================================================================
VITE_ENV=development

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================
# VITE_ENABLE_ANALYTICS=false
# VITE_ENABLE_DEBUG=true

# =============================================================================
# NOTES
# =============================================================================
# 1. All environment variables must be prefixed with VITE_ to be accessible in the app
# 2. For Docker: Use localhost:8000 since backend is exposed on host port 8000
# 3. For production: Use your actual API domain
# 4. Changes to .env require rebuilding the Docker image (npm run build)
# 5. Never commit sensitive data to version control
